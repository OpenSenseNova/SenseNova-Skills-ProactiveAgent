"""Packaged Harness connectors for Proactive Agent.

The source checkout historically imported ``acp`` directly after adding this
directory to ``sys.path``.  The distribution exposes the same code under the
stable ``sn_proactive_agent_connectors`` namespace, while the original checkout
layout remains available for demos and compatibility tests.
"""
